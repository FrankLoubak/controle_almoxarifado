/**
 * Finalidade: componente raiz da aplicação (placeholder Sprint 0).
 * Como funciona: renderiza uma tela mínima; as telas da seção 6 entram no Sprint 7.
 * Relações: montado por main.tsx; futuramente compõe rotas (Login, Ferramentas, etc.).
 */
export function App() {
  return (
    <main style={{ fontFamily: "system-ui", padding: 24 }}>
      <h1>Almoxarifado</h1>
      <p title="Esqueleto inicial — telas serão implementadas no Sprint 7">
        Setup concluído (Sprint 0).
      </p>
    </main>
  );
}
